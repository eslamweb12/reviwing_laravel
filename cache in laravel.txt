cache why we use cache?
 if  you want to optimize your app and make it faster if you want to show 10000 record of blog and it will take
 time for getting this all but not from database so you will use cache to store them 



**what is the diffrence between cache and session?
 1-cache is global so it is avaliable to all users of the application ,but the session is avaliable for only the user and i
 this session avaliable for only the use

 2-cache can be acced from any where in the app , but the session will be avalble for only the use 
  

 3-the cache faster than the session 


**how to use laravel Debugbar :to debug you app 

 1-composer require barryvdh/laravel-debugbar --dev
 2-php artisan vendor:publish --provider="Barryvdh\Debugbar\ServiceProvider"

 that is all and you will have a tab at the bottom of the


**how to use cache to store data 

public fuction index(){
 
  $post=Cache::remember('post',60,function(){
 
  return Post::with('category')paginate(5);


  });
}
post => is the key 
60 =>the time that the data will be deleted after it 

you will see the after you refresh the page of in the debugbar in queries no query as the data is saved in the 
cache but the time excuted you will see all queries that used 


** how to store data in the cache for ever 

public function index(){
 
 $posts= Cache::rememberForEver('post',function(){
    return Post::with('categories')->paginate(6);
 })
 return view('index',compact('posts'));
}



** how to delete data from cache
1-for locally and on production
 Route::get('forget-cache',function(){
 
  Cache::forget('post');
 });
 
if you visit this route the data will be deletd form the cache

2-if you run this commnd => php artisan cache:clear the data will be deleted from the cache  but this will use for locally 

**** 
how to use paginate with cache 

public function index(){
 $post= Cache::remember('post-page-'.request('page',1),60,function(){
   return Post::with('categories')->paginate(5);
    

});



